{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-cap",
  "description": "Capability for the overlay window",
  "windows": ["overlay"],
  "permissions": ["core:event:allow-listen"]
}
